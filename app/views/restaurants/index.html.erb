<h1>Restaurant List</h1>
<ul>
  <% @restaurants.each do |r| %>
    <li> <%= link_to r.name, restaurant_path(r) %> 
    - <%= r.address %> - <%= r.category %> </li>
  <% end %>
</ul>

<%= link_to "add a new restaurant", new_restaurant_path %>